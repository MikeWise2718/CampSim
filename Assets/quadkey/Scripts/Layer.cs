using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Aiskwk.Map
{
    public class Layer : MonoBehaviour
    {
        //Dictionary<string, GameObject> laygodict = new Dictionary<string, GameObject>();
        //public void AddGo(GameObject objgo)
        //{
        //    string key = objgo.name;
        //    if (laygodict.ContainsKey(key))
        //    {
        //        Debug.LogError($"layer {name} already contains a go named {key} - exiting");
        //        return;
        //    }
        //    laygodict[key] = objgo;
        //    objgo.transform.SetParent(transform,worldPositionStays:false);
        //}
        //public void Reattach(string key)
        //{
        //    if (!laygodict.ContainsKey(key))
        //    {
        //        Debug.LogError($"laygodict does not have a key called {key}");
        //        return;
        //    }
        //    var objgo = laygodict[key];
        //    objgo.transform.parent = null;
        //    objgo.transform.SetParent(transform, worldPositionStays: false);
        //}
        //void Start()
        //{

        //}

        //// Update is called once per frame
        //void Update()
        //{

        //}
    }
}